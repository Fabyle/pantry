For Spring 3.0

use the annotation called @Configuration 
and use the annotation called @inject ( use the jsr330 ) . 
The behavior is very similar compared with @Autowired. ( Autowired is Spring creation, and more rich, several options ). 